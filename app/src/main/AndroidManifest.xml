<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ikmr.banbara23.yaeyama_liner_checker"
    >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove"
        />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".core.ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".front.top.TopActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.portstatusdetail.PortStatusDetailActivity" />
        <activity
            android:name=".front.weather.WeatherActivity"
            android:label="@string/title_activity_weather"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".ui.portlisttab.PortListTabActivity"
            android:label="@string/title_activity_port"
            />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="df76d795ad6d60d4449a01b75a4be2b9469add8b"
            />
    </application>

</manifest>
